[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/ignis-server/Dockerfile"
buildContext = "."

[deploy]
startCommand = "/bin/bash apps/ignis-server/scripts/railway-entrypoint.sh"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
